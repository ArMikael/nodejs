<h1>
    Hello, {{ name }}
</h1>
<div>As we know you are, {{ userData.age }} years old and your occupation is {{ userData.job }}</div>

<h2>Condition</h2>
<section>
    {{# if condition }}
        <div>
            True section
        </div>
    {{ else }}
        <div>
            False section
        </div>
    {{/if}}
</section>

<h2>Array - For Each</h2>
<section>
    {{# each itemsList }}
        <div>{{ this }}</div>
    {{/each}}
</section>