<ul class="contacts-list">
    {{#each contactsList }}
        <li class="contact">
            <div class="name">
                {{ this.name }}
            </div>
            <div class="email">
                {{ this.email }}
            </div>
        </li>
    {{/each}}
</ul>